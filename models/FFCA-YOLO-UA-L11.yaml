# YOLOv5 🚀 by Ultralytics, GPL-3.0 license

# Parameters
nc: 2  # number of classes
depth_multiple: 0.67  # model depth multiple
width_multiple: 0.75  # layer channel multiple
anchors: 3

backbone:
  # [from, number, module, args]
  [[-1, 1, Conv, [64, 6, 2, 2]],  # 0

   [-1, 1, Conv, [128, 3, 2]],  # 1
   [-1, 6, C3, [128]], # 2
   [-1, 1, FEM, [128]], # 3

   [-2, 1, Conv, [256, 3, 2]],  # 4
   [-1, 6, C3, [256]], # 5
   [-1, 1, FEM, [256]], # 6

   [-2, 1, Conv, [512, 3, 2]],  # 7
   [-1, 9, C3, [512]], # 8
   [-1, 1, SPPF, [512, 5]],  # 9
  ]

head:
  [
   [-1, 1, Conv, [256, 1, 1]], # 10
   [[10, 6], 1, UAFeatureUpsample, [16, 32, 3, 2, 0.0]], # 11
   [[-1, 6], 1, FFM_Concat2, [1]], # 12
   [-1, 3, C3, [256, False]], # 13

   [-1, 1, Conv, [128, 1, 1]], # 14
   [-1, 1, nn.Upsample, [None, 2, 'nearest']], # 15
   [[-1, 3], 1, FFM_Concat2, [1]], # 16
   [-1, 3, C3, [128, False]], # 17

   [-1, 1, Conv, [128, 3, 2]], # 18
   [[-1, 6, 14], 1, FFM_Concat3, [1]], # 19
   [-1, 3, C3, [256, False]], # 20

   [-1, 1, Conv, [256, 3, 2]], # 21
   [[-1, 10], 1, FFM_Concat2, [1]], # 22
   [-1, 3, C3, [512, False]], # 23

   [17, 1, SCAM, [ ]], # 24
   [20, 1, SCAM, [ ]], # 25
   [23, 1, SCAM, [ ]], # 26

   [[24, 25, 26], 1, Detect, [nc, anchors]],
  ]
